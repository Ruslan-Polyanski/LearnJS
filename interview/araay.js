
() => {

  // Задача. Проверить, совпадают ли массивы чисел

  // isSimilar([0, 1, 2], [2, 1, 0]) === true
  // isSimilar([0, 1], [2, 1, 0]) === false
  // isSimilar([0, 5, 1], [0, 1, 5]) === true

  function isSimilar(arr1, arr2) {
    // Ваш код
  }

}


